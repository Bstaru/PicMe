
/*
$(".onoffswitch-checkbox").on( 'change', function() {
     
 //  var val = $("#myonoffswitch").is(':checked'); //true
    
    if( $(this).is(':checked')  ) {
      // $("#editarUsuario").submit();
        alert("El checkbox con valor " + $(this).val() + " ha sido seleccionado");
    } else {
        alert("El checkbox con valor " + $(this).val() + " ha sido deseleccionado");
    }
}); 



function checharCosa() {
       
       var checado =  $('#myonoffswitch:checked').length; // "1"
       var priv;
         
       if (checado === "1"){
           priv = true;
           alert("es true");
       }
       else{
           priv = false;
           
           alert("es false");
       } 
}; */